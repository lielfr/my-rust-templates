# My Rust templates

I created these to hopefully save me some time in projects. If you'd like, you can also use them.

## Usage

Make sure you have `cargo-generate` installed. Then:

```bash
$ cargo generate git@github.com:lielfr/my-rust-templates.git <template name>
```

You will then be asked different questions to customize the template.
